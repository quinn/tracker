use rocket::{get, serde::json::Json};
use rocket_okapi::openapi;
use sea_orm_rocket::Connection;

use crate::pool::Db;

#[openapi]
#[get("/{{name}}")]
pub fn {{name}}(conn: Connection<'_, Db>) -> Json<String> {
    Json("".to_string())
}
